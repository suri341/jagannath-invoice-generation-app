apiVersion: v1
kind: Service
metadata:
  name: invoice-frontend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: invoice-frontend
  ports:
    - port: 80
      targetPort: 80
      name: http
